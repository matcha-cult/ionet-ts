{
  "name": "demo-browser-client",
  "version": "0.1.0",
  "private": true,
  "description": "ionet browser reference client: zero-framework-dependency WebSocket + fetch demo (PROTOCOL.md compliant)",
  "author": "NbbJack",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "smoke": "tsx smoke/smoke.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.22.0",
    "typescript": "^5.4.0"
  }
}
