{
  "name": "ionet-ts",
  "version": "0.1.0",
  "private": true,
  "description": "ionet TypeScript port — distributed network programming framework",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "eslint packages/*/src --ext .ts",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "reflect-metadata": "^0.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "overrides": {
      "esbuild": "0.21.5"
    }
  }
}
